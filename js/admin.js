jQuery( document ).ready( function() {

	/* === Radio Image Control (Post Layout) === */

	if ( jQuery.isFunction( jQuery.fn.buttonset ) ) {
		jQuery( '#hybrid-post-layout .buttonset' ).buttonset();
	}

} ); // jQuery( document ).ready